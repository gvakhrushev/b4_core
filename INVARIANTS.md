# Traceability: SECURITY_MODEL §2 invariants → tests

Layers: **U** unit/regression (`test/unit`), **I** integration (`test/integration`),
**S** stateful campaign (`test/invariant`, default 64×64, nightly `FOUNDRY_PROFILE=deep`
512×256). GAP markers are honest statements of what is *not* locally proven.

Rows 1–18 are `SECURITY_MODEL.md` §2 invariants 1–18. Rows 19–22 are properties added by later
audit rounds and traced here before being renumbered into §2 — 19/20 by AUDIT-2026-07-25 (C-1,
H-3), 21 by its L-1 sleeve-reachability fix, 22 by its "half B" fix round. §2's own invariant 19
(strict Product Pool penalty routing) is row **23**: the numbering diverged when rows 19/20 were
added before §2 gained a 19th entry.

| # | Invariant | Tests | GAP / residual |
|---|---|---|---|
| 1 | One execution identity ⇔ one vault | U `test_createVault_atomicBinding` (identity = clone address, bound at init) | Structural (EVM address uniqueness); no runtime assert needed |
| 2 | No cross-vault movement authority | U `test_rogue_pool_and_vault_isolation`; I `test_multivault_independence`; S `invariant_config_immutable` | **GAP:** Core-side msg.sender scoping of CoreWriter actions is venue semantics — funded gate §5.10 |
| 3 | No accounting from unmeasured transfers | U `test_donation_not_accounted_recoverable`, `test_capture_measuredDelta_only`, `test_R7_fund_credit_capped`; S `invariant_books_never_exceed_assets` | — |
| 4 | Action success never finalizes accounting | U all `test_R1_*`, `test_R2_delayed_action_not_doubleApplied`, `test_spotOrder_*` | Live venue timing not reproducible locally — funded gates §5.8–10 |
| 5 | Core→EVM completion = debit + receipt | U `test_A7_return_never_resends_after_debit`, `test_R1_return_completes_despite_small_topUp` | — |
| 6 | Spot credit ≤ proven input × envelope | U `test_spotOrder_favorable_overfill_stays_unaccounted`, `test_spotOrder_partialFill_measured` | — |
| 7 | Harvest bounds; claim ≤ one-call settleable | U `test_harvest_bounds`, `test_R3_harvest_quota_deadlock_clamps_and_clears`, `test_R3_harvest_quota_withdrawable_zero_liquidation`, `test_R3_pending_claim_gates_nothing`, `test_R3_adversarial_drain_between_create_and_execute`; S runtime proxies `invariant_crank_never_reverts` + `invariant_books_never_exceed_assets` | The precise A5 quota-coexistence discipline (`pendingHarvest6 == 0` while its FromPerp/Harvest resolver is in flight) is proven deterministically by the `test_R3_*` harness tests. A dedicated campaign invariant was **removed as vacuous** (V3-COV-1): the adversarial `advWdDrain`/`advLiquidation` handlers keep the campaign out of the harvest-accrual state (`pendingHarvest6 > 0` observed 0 times over 4,096 calls), so the state it asserts is never reached |
| 8 | Owner margin separate from strategy capital | U `test_promax_opens_leveraged_long` (strategy value excludes margin; notional ≤ margin·maxLev/φ) | — |
| 9 | Sign change passes through verified zero | U `test_promax_sign_change_passes_through_zero`; S `invariant_no_sign_flip` | — |
| 10 | Pool liability by receipt; balance ≥ liability | U `test_capture_measuredDelta_only`, `test_claim_proRata_paysFixedOwner`; S `invariant_pool_balance_ge_liability` | — |
| 11 | Distribution ≤ nominal; order-independent socialization | U `test_shortfall_socialization_orderIndependent`, `test_failed_transfer_leaves_token_retryable` | — |
| 12 | Policy/scale change never exit/penalty | U `test_policyChange_noExit_noPenalty`; S ghost `policyMovedFunds` set in the `selectPolicy` handler, checked by `invariant_policy_never_moves_funds` (a ghost, not an in-line assert — a handler-local revert would be masked under `fail_on_revert = false`, RAW-E-001) | — |
| 13 | Emergency never discards an asset transfer **whose funds still exist** — and, where they provably do not, records the loss instead of freezing the vault (A6, corrected 2026-07-30). `emergencyClearRecovery` still admits `Recover*` only. `abandonStuckReturn` is the single exception and is gated on the Core source having actually DECREASED, so a slow leg — funds still on Core, resend branch still live — is refused (`ReturnNotStuck`) exactly as before | U `test_R4_asset_transfer_intents_not_discardable`, `test_R4_abandon_phase1_then_rerecover`, `test_R4_abandon_phase2_then_rerecover_via_spot` | — |
| 14 | Withdrawal: strict-flat NAV + full Core return before payment | U `test_R5_strict_flatness_blocks_margin_return`, `test_exit_returns_core_principal_before_payment`, `test_R5_recovery_requires_raw_zero` | Reduce-only-to-raw-zero on the live venue — funded gate §5.11 |
| 15 | Fee route immutable after creation | U `test_route_immutable_no_setter_exists`, `test_route_validation_matrix` (no setter exists in the ABI); S `invariant_config_immutable` | — |
| 16 | Multi-vault independence (same owner) | I `test_multivault_independence`; S per-vault book checks + `invariant_config_immutable` | — |
| 17 | Completion/retry on reliable balance only; resend = exact complement | U `test_R1_fromPerp_completes_under_withdrawable_drift`, `testFuzz_R1_fromPerp_completion_ignores_withdrawable`, `test_R1_toPerp_completes_despite_small_topUp`, `test_R2_subAmount_topUp_does_not_block_resend`, `test_R2_fromPerp_dropped_resend_reclamps`, `test_R7_source_topUp_equal_amount`, `test_R7_destination_topUp_fakes_completion_once_benign` | Delayed-double-execution across a resend excluded by assumption — funded gate §5.10 |
| 18 | Safe permissionless entrypoints; worst case = delayed liveness, **except** a Core→EVM credit lost by the venue, which is a realized loss no contract can undo (A7). Bounded, not denied: the vault itself is not frozen with it — see row 21 | U `test_onlyOwner_guards`, `test_claim_proRata_paysFixedOwner` (fixed recipient), pool bounded-asset loops at the MAX_DIRECTIONAL cap `test_V3Cov_max_directional_pool_lifecycle_and_cap` (8-directional pool full cycle + 9th-directional cap revert); U `test_missed_snapshot_makes_interval_unreportable_not_stuck`, `test_superseded_point_skipped`; U payout-defer under recipient blacklist: `test_settle_with_blacklisted_operator_defers_not_freezes`, `test_exit_with_blacklisted_operator_completes`, `test_keeper_retries_deferred`; S `invariant_crank_never_reverts`, `invariant_pool_advance_never_reverts` (the permissionless calendar-advance step, exercised by every `poolCrank()`), `reconcileHeals` handler | — |

| 19 | **Weight integrity: reported weight tracks real, earned profit — and the instant it is measured at is not a caller's choice.** Measured interval profit MUST equal the vault's own P&L against what it actually paid — entry ledger and NAV on one price basis (`HAZARDS.md` B4) — so weight can never be created by capital that did not earn it. The NAV and its price MUST be captured together at one instant by the one-shot `B4Vault.snapshotNav`, confined to `Calendar.SNAPSHOT_WINDOW`; settle values off that capture and refuses (`NavNotSnapshotted`) rather than substituting a later price. Because the capture is permissionless the owner pre-empts it at `pointTime`, which is what bounds AUDIT-2026-07-29 F4 — settle used to value at the instant IT ran, anywhere in the 3-day report window, so a third party pinned another vault's weight at a trough with no second attempt for the victim. The captured price is also the basis the in-kind operator cut values the basket on. **A frozen snapshot MUST be kept consistent with every capital movement that lands before settle**, in both directions: `deposit` raises it by the deposited value (A1) and `_finalizeExit` scales it by the same `keep` the ledgers use (A5). Freezing a NAV creates this obligation — the window is reachable by both, and the settlement point sits inside a `freeExit` zone, so an exit there is penalty-free as well. Left stale, one direction drops principal that resurfaces as phantom profit, the other retains withdrawn principal that reads as profit outright (measured: a 50% exit made settle take 80k where 15k was real). **And settle MUST cap the frozen NAV at the recorded books valued at the SAME frozen price** (A7): per-mover hooks close instances, the cap closes the class — a realized loss lowers the books with no hook at all, and no future mover can raise the settled NAV above what the vault holds. The cap must use the frozen price, never the live one, or the settle caller regains the price choice F4 removed; the upward direction still needs A1, since a cap cannot invent value the snapshot never recorded | U `test_C1_post_lock_directional_deposit_mints_no_weight`, `test_C1_usdc_deposit_then_crank_rotation_mints_no_weight`, `test_C1_deposit_free_exit_loop_mints_no_weight`, `test_C1_real_holding_still_earns_weight` (positive control), `test_settle_values_at_live_price_real_pnl`, `AuditF4_SettleValuationInstant.t.sol` ×8, `AuditA_ClosureFixes.t.sol` (A1) | **GAP:** no campaign invariant reads `entryLedgerWad` / `rewardBaseWad` / `weightOf` / `totalWeight`. This row did not exist before AUDIT-2026-07-25, which is why C-1 survived eight rounds: nothing asserted the property at all. Settlement-instant discretion is a disclosed residual (`SECURITY_MODEL` §3) |
| 20 | **A zero directional price is never a basis or a valuation** — deposit rejects, settle rejects, exit defers-and-stays-cancellable **for every vault, including a pool-owned sleeve** | U all `test_H3_*` (8); U `test_L1_sleeve_exit_is_cancellable_under_a_dead_feed`, `test_L1_sleeve_exit_cancel_refused_while_the_feed_is_live`, `test_L1_dead_feed_cancel_refused_when_the_sleeve_holds_no_directional`, `test_L1_cancel_restores_foldPenalty_only_once_the_feed_returns` | Deferral is delayed liveness; `cancelExit` is the escape from a permanently dead feed. A sleeve's owner IS the pool, so its escape is `B4Pool.cancelSleeveExit`, gated on the exact complement of `_finalizeExit`'s deferral predicate — before that forwarder existed this row was false for every sleeve (audit L-1 / review NR-4). **Honest bound:** cancelling frees the sleeve's planner and un-sterilizes `foldPenalty`, but a *directional* fold stays blocked while `pxWad == 0` by `deposit`'s own H-3 guard |
| 21 | **Every surplus class a vault accrues is reachable by its owner, and no intent can freeze the vault forever** — `recoverEvm` / `recoverCoreSpot` / `recoverPerpSurplus` / `emergencyClearRecovery`, plus `abandonStuckReturn` for the one asset-transfer leg that can become unrecoverable (HAZARDS A6/B6, decision C1). A `Return*` whose source decreased can never resend (A7), so a permanently lost credit used to freeze every idle-gated entrypoint with no admin to unstick it; after 30 days the owner writes the books down to the real Core balance and continues, and a late arrival still lands on the recoverable path. The escape MUST refuse while the source still holds the funds — that leg is slow, not wedged. For a pool-owned sleeve the owner is the pool, which relays all four with fixed arguments and folds the proceeds into ordinary claim inventory | U `test_R6_*` ×4, `test_R4_*` ×4 (EOA owner); U `test_L1_sleeve_evm_surplus_becomes_claim_inventory`, `test_L1_sleeve_core_spot_surplus_becomes_claim_inventory`, `test_L1_sleeve_perp_surplus_becomes_claim_inventory`, `test_L1_clearSleeveRecovery_unsticks_a_dropped_recovery_leg`, `test_L1_sleeve_escape_forwarders_admit_no_caller_discretion` | The relay adds no privileged fund mover: the recipient is the vault's immutable `owner`, the amount is the vault's own `balance − recorded` bound, and the pool admits it only by measured delta (D2). Non-whitelisted tokens in a sleeve are out of reach by design — relocating an undrainable balance into the pool helps no one (audit L-1) |
| 22 | **Reported pool weight always tracks the capital still standing behind it.** Both sides of the ledger scale by the SAME `keep = 1 − x` on EVERY exit: the SPEC §9 formula `nextRewardBase = (R + C·x)·keep` scales the vault-side base, and `B4VaultOps._finalizeExit` calls `B4Pool.scaleWeight(lastSettledPlusOne − 1, keep)`, which scales the weight already reported for the open interval and lowers `totalWeight` by the same amount. A full exit (`keep = 0`) is the endpoint of that ramp, not a special case: it zeroes both. The basket is funded by leavers for the benefit of stayers, so a claim must never outlive the capital that earned it, and the two call orders (`settle`→`exit`, `exit`→`settle`) must agree | U `AuditHalfB_ExitWeightOrder.t.sol` ×6; U `Exit.t.sol` (`test_full_exit_outside_free_window` asserts the zeroed base; `test_partial_exit_ledger_math` and `test_repeated_partial_exits_no_weight_duplication` pin the `(R + C·x)·keep` contraction); U `V6B_ExitFairness.t.sol` (iv) pins the per-exit scaling and the window confinement | **AUDIT-2026-07-29 F1:** the pool side used to fire only at the exact boundary `keep == 0`. Because that is an equality test on a number the owner chooses, `initiateExit(WAD − 1)` paid out every unit of the position but flooring dust and kept **100%** of the reported weight, collecting a full pro-rata share of a basket funded by other participants' penalties. No threshold fixes an exact-boundary test — every threshold has its own "just above" — and a measure taken from the post-exit BASE is worse, because `(R + C·x)·keep` is re-inflated by the exiting share's own unsettled profit, whose timing the owner controls. Proportional scaling removes the boundary instead of moving it, and repeated exits compound multiplicatively because each call re-reads the live weight. **Residual, asserted not hidden:** a vault that settles AFTER emptying itself has `entryLedgerWad == 0`, so flooring dust left by the exit waterfall reads as profit and reports a dust weight; bounded below 1 ppm of a real participant's share by the order-agreement test. An exit finalized AFTER `reportDeadline` scales nothing, because `scaleWeight` is confined to `block.timestamp <= reportDeadline(id)`, disjoint from the claim window, so `totalWeight` never moves while claims are open (D2/D3) — the rule therefore holds inside the report window, not past it. Every non-applicable case is a silent no-op, never a revert on the permissionless crank path (H3) |
| 23 | **(= `SECURITY_MODEL` §2 invariant 19) Strict Product Pool penalty routing.** A non-free exit's MEASURED penalty can fund only its immutable matching `(product, directional asset)` sleeve, whose address is write-once at pool creation; live sleeve escrow is not claim liability and is excluded from the shortfall balance, joining ordinary distribution only after that sleeve's free-window exit; no caller can choose another strategy, recipient or a live cross-product transfer; a different whitelisted token at the pool is ordinary donation inventory, never penalty escrow (`HAZARDS.md` D6/D7, `ARCHITECTURE.md` strict-product section) | U `AuditH1_PenaltyReceipt.t.sol`; U `ProductPools.t.sol` ×7 | **GAP (narrowed):** the legacy campaign still drives a `policyMask == 0` pool; the strict escrow/sleeve discipline is covered by `StrictPool.invariant.t.sol` and the deterministic tests. Crank-timing discretion over the moment `foldPenalty` opens a sleeve position is a disclosed residual (`SECURITY_MODEL` §3) |

## Mandatory regressions (TEST_PLAN §2–4) — checklist

| §2 regression | Test(s) |
|---|---|
| 1 Reliable-balance completion (A2) | `test_R1_*` ×3 + fuzz |
| 2 Exact-complement resend (A3) | `test_R2_*` ×3 |
| 3 Harvest-quota deadlock incl. wd == 0 (A4/A5) | `test_R3_*` ×4 |
| 4 Emergency recovery vs. discard (A6) | `test_R4_*` ×4 |
| 5 Strict custody flatness (A10) | `test_R5_*` ×2 |
| 6 Surplus recovery spot AND perp, two-phase abandon (B6) | `test_R6_*` ×4 + `test_R4_abandon_*` |
| 7 Top-up == and > amount (A11) | `test_R7_*` ×3 |
| 8 Realized-loss reconciliation in settle/exit/sync (B2) | `test_R8_*` ×2 + `test_sync_reconciles_loss_before_sizing` |
| §3.9 Checkpoint-price lock (D1 — rewritten) | `test_checkpointPrice_zero_on_one_asset_cannot_block_the_pool`. The old regression `test_checkpointPrice_poisoning_transientZero_retries` **no longer exists and must not be cited**: since C-1 the recorded `lockedPxWad` feeds no valuation, so poisoning it mis-prices nothing, and the all-or-nothing refusal it guarded became pure liveness cost — one dead co-listed feed would have blocked reporting and claiming for the whole pool. The property asserted now is the stronger one: a transient zero can neither poison a valuation nor stop the interval (`lockedAt` still commits, the healthy asset is still recorded, the dead one is simply absent) |
| §3.10 balance ≥ liability + socialization (D2/D3) | `test_shortfall_socialization_orderIndependent` + S campaign |
| §3.11 Halving bootstrap/acceptance, fast cycle, no wall clock (E1/E2), key continuity | `test_bootstrap_requires_proven_fact`, `test_bootstrap_header_is_bound_and_idempotent`, `test_accepts_ultraFast_cycle_noWallClockWindow`, `test_intervalKey_continuity_across_epoch_boundary` |
| 20-day transition / late day-15 entry reaches full day-20 target | `test_promax_opening_fall_day15_half_then_day20_full`, `test_promax_opening_growth_day15_half_then_day20_full`, `test_promax_late_entry_on_fall_day15_reaches_full_day20`, `test_promax_late_entry_on_growth_day15_reaches_full_day20` |
| Anchor density counts daily observations, not calls | `test_density_gate_ignores_same_timestamp_repeats`, `test_density_gate_ignores_subday_repeat_spam` |
| Strict product escrow has no cross-directional dead slot (D6/D7 eligibility: only settlement + the exiting vault's own directional token) | `test_non_matching_whitelisted_token_stays_generic_inventory` |
| §3.12 Cross-chain bindings (E3) | `test_rejects_untrusted_paths`, `test_idempotent_and_conflicting_redelivery`, `test_delegate_renounce_oneShot`, `test_prover_publishes_bound_fact` |
| §3b.13 Equal-target no-trade / same-sign interpolation | `test_mini_never_trades` (all-transition sweep), `testFuzz_sameSign_direct_interpolation` (non-equal same-sign direct path), `test_zero_endpoint_piecewise`; fee-on-profit without trades: `test_settle_profit_fee_weight` (Mini) |
| §3b.14 Dust-exit weight minting | `test_repeated_partial_exits_no_weight_duplication` (10 dust exits + honest 50%), `test_partial_exit_ledger_math` |
| Settle-mid-flight entry integrity (B1/B4) | `test_settle_requires_idle_then_no_phantom_profit` — the settle-requires-idle hardening (RAW-A-001) means a valuation never runs while funding is in flight, so returning principal can never read as profit. (Prior map entry named `test_settle_midFundFlight_no_phantom_profit`, which never existed — corrected per V3-COV-1.) |
| Payout-defer liveness (H3; USDC blacklist is in-model) | `test_settle_with_blacklisted_operator_defers_not_freezes`, `test_exit_with_blacklisted_operator_completes`, `test_deferred_not_recoverable_as_surplus`, `test_keeper_retries_deferred` |
| Settle requires idle; reconcile at idle only (B2 dual trap, RAW-A-001) | `test_A001_settle_requires_idle_during_marginReturn`, `test_settle_requires_idle_then_no_phantom_profit`, `test_A001_marginReturn_value_conserved_at_idle`, `test_A001_coincident_loss_reconciled_at_idle_before_settle`; real-loss reconcile at settle preserved by `test_R8_settle_reconciles_realized_loss_before_valuation` |
| Order-size overflow safety (RAW-D writer, adversarial) | `test_D_writer_units_no_overflow_micro_asset` (clamp, no revert), `test_D_writer_units_asymmetric_szDecimals` (mock-collusion guard), `test_D_binding_rejects_bad_decimals_and_widths` |
| Spot-only perp-policy degradation (RAW-D NO_MARKET) | `test_D_spot_only_perp_policy_degrades_and_recovers` (Pro on spot-only: rotate-only, inert margin paid at exit, perp surplus recoverable) |
| Harvest claim reserved from surplus recovery (AUDIT-1, C1) | `test_AUDIT1_recoverPerpSurplus_reserves_harvest_claim`; stressed by the `recover` handler in the stateful campaign |
| Pool sibling reentrancy guard (AUDIT-2, F4) | `test_AUDIT2_pool_siblings_nonreentrant_during_claim`, `test_AUDIT2_exit_capture_still_works` |
| Malformed-token fail-soft (D5, RAW-B-001) | `test_B001_tryTransfer_never_reverts_on_malformed_return`, `test_B001_pool_claim_isolates_malformed_token` |
| CoreWriter fixed-1e8 units (RAW-D scales; funded gates §5.4–5) | `test_D_spot_order_encodes_fixed_1e8_units`, `test_D_perp_order_encodes_fixed_1e8_units` (exact emitted-calldata) |
| Spot-only descriptor lifecycle (RAW-D NO_MARKET) | `test_D_spot_only_vault_full_lifecycle` (mock fails invalid-asset position reads) |
| Extended perp-id rejection (RAW-D HIP-3) | `test_D_hip3_wide_perp_id_rejected` |
| §4.15 Permissionless safety | `test_onlyOwner_guards`, fixed-recipient claim tests |
| §4.16 Atomic init / re-init guards | `test_reinit_guarded_oneShot`, `test_implementation_cannot_be_initialized`, `test_createVault_atomicBinding` |
| §4.17 Rogue pool/vault isolation | `test_rogue_pool_and_vault_isolation` |
| C-1 JIT checkpoint weight — the deposit, USDC-rotation and free-exit-loop shapes, plus a positive control. **Every one of these MUST crank between the deposit and the settle**: a version that skips the crank passes while the exploit is live | `AuditC1_JitWeight.t.sol` ×4 |
| H-3 zero directional price on the two ledger-WRITING paths, and the `cancelExit` escape | `AuditH3_ZeroPrice.t.sol` ×8 |
| C-2 an out-token donation cannot complete a live IOC (A2: completion keys on a self-caused DECREASE only); the no-fill path still clears on timeout | `AuditC2M1_AsyncCompletion.t.sol` ×2 |
| M-1 a zero re-clamp on a `Return` leg clears instead of wedging both predicates forever (A12) | `test_M1_zero_reclamp_clears_instead_of_wedging_forever` |
| H-1 `capturePenalty` escrows only the exit's MEASURED receipt, never the pool's whole unattributed balance; a co-resident donation stays claim inventory and `balance >= liability + escrowHeld` holds | `AuditH1_PenaltyReceipt.t.sol` (fail-before: escrows donation + penalty, 61,803,398 vs the 11,803,398 penalty) |
| H-4 the L-halving long anchors on the promoted 62-min (`floor`), not the current window's running low; asserted on the realized LIQUIDATION PRICE per STRUCTURAL-STATE-MACHINE §3 row PM5 | `test_H4_l_halving_long_liquidates_at_the_62min_derived_stop`; clamp scenarios re-seeded via `_seedFloorViaHalvingFlip` in `V8A_Liquidation.t.sol` / `V9Engine.t.sol` |
| M-3 the PEAK value is the **max over daily CLOSES, corroborated by two distinct closes** (SPEC §152 "max close"). An observation binds only inside `ANCHOR_CLOSE_WINDOW` of a close, on a day grid fixed to the peak window's own opening, and a level is served or promoted only once TWO distinct close-days reach it. So: an off-close wick cannot bind at all; a single at-close print is a candidate and nothing more; and — because value binding is independent of the density counter — no caller can suppress the true high by owning the daily slot. The window OPENING carries no served value either. The LOW is deliberately NOT mirrored: it ratchets on every observation, because a lower low pushes a long's stop further out and LOWERS leverage (the conservative direction), and every-observation is a superset of daily closes in exactly that safe direction. Only the density COUNT is daily on both sides, and it gates confirmation, never value. Because the two are now separate, a window can be density-confirmed while NO close was ever corroborated (`peakC == 0`) — a keeper sampling daily but always mid-day does exactly that. Fail-safe for the engine (a zero peak reads as absent → flat base, and the promotion carries the zero rather than a stale higher `Pp`, which widens `(C − Pp)` and lowers leverage), so `anchorConfirmed`'s peak leg reports the SERVED value rather than the count — otherwise an operator dashboard shows a healthy anchor while the product runs unanchored for a cycle | `test_M3_intraday_wick_cannot_move_the_confirmed_peak`, `test_M3_intraday_crash_moves_the_low_immediately`, `test_M3_next_day_observation_ratchets_once_corroborated`, `test_M3_corroboration_survives_a_later_lower_close`, `AuditF2_PeakSlotSquat.t.sol` ×5, `AuditF2_ConfirmedWithoutValue.t.sol` |
| half B an exit leaves a claim proportional to what it kept: both the vault-side base and the already-reported pool weight scale by the same `keep` (`B4Pool.scaleWeight`), so a full exit leaves NO claim and a PARTIAL exit surrenders exactly the share withdrawn; settle-then-exit and exit-then-settle agree; repeated exits compound multiplicatively and stay bounded by one settle's worth. **Reversed by AUDIT-2026-07-29 F1:** this row previously read "a PARTIAL exit forfeits nothing", which made the pool side an exact-equality test on `keep == 0` and let `initiateExit(WAD − 1)` keep the whole claim on dust capital. See invariant 22 | `AuditHalfB_ExitWeightOrder.t.sol` ×6 |
| The close is REALISED: 1/10 per day across the closing leg at whatever each day prints, fully in fiat at the settlement point, profit = actual proceeds − actual deposit, and an absurd mark moves nothing | `RealizedClose.t.sol` |
| Settlement descriptor must BE the venue quote asset — core token index 0 (a non-quote settlement token freezes every perp-bearing vault it creates) | `AuditObs2_SettlementIdentity.t.sol` ×5 (both factories, a positive control that the same token still binds as a directional, and the freeze it prevents). The `test_F3_*` pair in `AuditV5Fixes.t.sol` covers only `verifySettlement`'s `fixedUsd` and `coreWeiDecimals` branches — it never touched this one |
| L-4 activation allowance pinned at intent creation, never re-derived at a later price | `B4VaultEngine._startFund` / `_verifyFund` |
| A zero price on one co-listed asset can no longer block the whole pool's reporting | `test_checkpointPrice_zero_on_one_asset_cannot_block_the_pool` |
| EIP-170 on every deployed contract, printing headroom (the previous guard covered one contract while two sat within ~100 bytes) | `Eip170Sizes.t.sol` |
