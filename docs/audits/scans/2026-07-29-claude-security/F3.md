# F3: Permissionless claimDeferred races an in-flight Core-to-EVM return leg and permanently wedges the vault

**Status:** patch written -> `F3.patch`

**Verified by a panel of agents.** An independent verifier reviewed the change against the finding and stated the three claims below with confidence, and a second, fresh reviewer re-challenged the bare diff for new vulnerabilities. The patch was written only because the panel could vouch for it; nothing here was applied for you.

`_unaccountedEvm` counted value the vault holds but owes, so a permissionless claim lowered the balance without lowering the subtrahend and destroyed the delivery receipt; the patch adds `deferredPayoutTotal[token]` to `booked` so a claim moves both sides by the same amount and cancels exactly.

## Confidence

- **the change is highly targeted to this finding** -- CONFIDENT: the whole executable diff is one expression at src/core/B4VaultEngine.sol:417-418; the rest is the docstring that asserted the disproven invariant plus one regression test
- **the change introduces no new security vulnerability** -- CONFIDENT: the term cannot be raised mid-leg (its only writer is idle-gated) and its only decrement transfers the identical amount out in the same call, reverting both together on failure; a fresh adversarial reviewer of the bare diff found no attack path
- **beyond closing the finding, the change does not alter the code's behaviour or the inputs it accepts** -- CONFIDENT: only deltas of the measure are consumed and the added term is constant across a leg; an owner-recovery probe passed identically on base and patched, and all 454 pre-existing tests pass unchanged

**Tests run:** forge test -> 456 tests passed, 0 failed, 0 skipped across 77 suites, including invariant_crank_never_reverts (4096 calls each campaign); forge build --sizes; both added tests confirmed to fail against base sources

## Change

- `src/core/B4VaultEngine.sol` (+21 -4)
- `test/unit/DeferredClaimReturnRace.t.sol` (+136 -0)

## Applying it

Applies cleanly to the working tree (checked with `git apply --check`).

```
git apply CLAUDE-SECURITY-20260729-140307/patches/F3.patch
```

Or ask Claude Security to apply it, or to open a pull request for it.
